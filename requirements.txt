# --- pipeline ---
playwright>=1.44        # authenticated course crawling in your own browser session
yt-dlp>=2024.0          # video/audio download (Wistia, YouTube, Vimeo, etc.)
mlx-whisper>=0.3 ; platform_system == "Darwin" and platform_machine == "arm64"
openai-whisper>=20231117 ; platform_system != "Darwin" or platform_machine != "arm64"
requests>=2.31
beautifulsoup4>=4.12
markdownify>=0.11
openpyxl>=3.1
python-pptx>=0.6
python-docx>=1.1
pyyaml>=6.0
# --- local LLM distillation via Ollama HTTP API (no python dep; install Ollama separately) ---
# --- web UI ---
streamlit>=1.36
